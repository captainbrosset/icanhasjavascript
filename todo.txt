

	js source code base
		parse each file
			extract
				list of functions
				list of classes
				list of methods per class
				list of field per class
				global variables
		build a model out of this
		re-run the parsing on file save
		parse in a separate thread
		allow to update the model continuously while the editor is being used
	plug in the contextual menu to provide list of names, filtered by the beginning of the word
		show the suggestion + the file name where it comes from + class name if any
